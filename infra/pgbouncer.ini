# Seed stub created by scripts/seed_demo_repo.py
# Establishing-decision PR: infra: route all Postgres access through PgBouncer (transaction pooling)
# Touches: infra/pgbouncer.ini
# The real rationale ('why') lives in the linked incident issue, not this diff.
